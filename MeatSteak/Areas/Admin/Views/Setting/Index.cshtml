@model PaginationVM<Setting>
@{
    int count = (Model.CurrentPage - 1) * 3;
}
<div class="col-lg-12 grid-margin stretch-card">
    <div class="card">
        <div class="card-body">
            <h4 class="card-title">Setting Table</h4>
            <div class="table-responsive">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>
                                No
                            </th>
                            <th>
                                Key
                            </th>
                            <th>
                                Value
                            </th>
                            <th>
                                Settings
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (Setting setting in Model.Items)
                        {
                            count++;
                            <tr>
                                <td>
                                    @count
                                </td>
                                <td>
                                    @setting.Key
                                </td>
                                <td>
                                    @setting.Value
                                </td>
                                <td>
                                    <a class="btn btn-warning" asp-action="update" asp-controller="setting" asp-route-id="@setting.Id">Update</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
                <nav aria-label="Page navigation example">
                    <ul class="pagination">
                        <li class="page-item @(Model.CurrentPage-1<=0?"disabled":"")"><a class="page-link" asp-action="index" asp-controller="setting" asp-route-page="@(Model.CurrentPage-2)">Previous</a></li>
                        @for (int i = 1; i <= Model.TotalPage; i++)
                        {
                            <li class="page-item @(Model.CurrentPage<=i?"disabled":"")"><a class="page-link" asp-action="Index" asp-controller="setting" asp-route-page=@(i-1)>@i</a></li>
                        }
                        <li class="page-item @(Model.CurrentPage==Model.TotalPage?"disabled":"")"><a class="page-link" asp-action="index" asp-controller="setting" asp-route-page="@Model.CurrentPage">Next</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </div>
</div>